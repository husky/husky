#!/bin/bash
# this file is generated by robot_bringup/upstart/install.bash
# substitutions: interface0, robot, job, release

log_file=/tmp/robot-upstart.log
DATE=`date`
echo "$DATE: robot job stop" >> $log_file

# echo "$DATE: robot job using setup file /etc/ros/setup.bash" >> $log_file
# source /etc/ros/setup.bash

# for i in $( rosnode list ); do
#    rosnode kill $i;
# done

# killall roslaunch
